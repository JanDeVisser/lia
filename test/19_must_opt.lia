struct X {
    foo: i64,
    bar: i64,
}

func ret_opt(ok: bool) ?X {
    if (!ok) {
        return null
    }
    x: X = { 69, 420 }
    return x
}

public func main() i32 {
    x := must ret_opt(true)
    putint(x.bar)
    endln();
    x = must ret_opt(false)
    return 0::i32
}

